# Local dev: Vite proxies /api → backend (see vite.config.js)
VITE_API_BASE=/api
VITE_PROXY_TARGET=http://127.0.0.1:8000

# Production (set in Vercel UI only, not committed):
# VITE_API_BASE=https://YOUR-SERVICE.onrender.com
